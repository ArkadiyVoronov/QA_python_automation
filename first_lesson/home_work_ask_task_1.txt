[x] Какие типы данных есть в языке Python?
Неизменяемые:
Numbers (числа):
-int (целое число)
-long (длинное целое число)
-float (число с плавающей точкой)
-complex (комплексное число)
Boolean (логический тип данных)
Strings (строки)
Tuples (кортежи)
-составной тип данных, для экономии места в памяти
-элементы кортежей не могут быть изменены
Изменяемые:
Lists (списки)
-самый универсальный составной тип данных в Python
Dictionaries (словари)
-неотсортированная коллекция элементов,
-доступ к которым осуществляется по ключу
Sets (множества)
изменяемый, коллекционный тип данных,
отличительной чертой которого является то,
что он хранит только уникальные значения.

[х] Есть ли в Python возможности для объектно-ориентированного программирования (ООП)?
(ООП) — парадигма программирования, в которой основными концепциями являются понятия объектов и классов.
Класс — тип, описывающий устройство объектов.
Объект — это экземпляр класса.
Класс можно сравнить с чертежом, по которому создаются объекты.
Python соответствует принципам объектно-ориентированного программирования.
В python всё является объектами - и строки, и списки, и словари, и всё остальное.
Но возможности ООП в python этим не ограничены. Программист может написать свой тип данных (класс), определить в нём свои методы.
>>> # Пример простейшего класса, который ничего не делает
... class A:
...     pass
>>> a = A()
>>> b = A()
>>> a.arg = 1  # у экземпляра a появился атрибут arg, равный 1
>>> b.arg = 2  # а у экземпляра b - атрибут arg, равный 2
>>> print(a.arg)
1
>>> print(b.arg)
2
[х] Что такое динамическая типизация?
Динами́ческая типиза́ция — приём, используемый в языках программирования и языках спецификации,
при котором переменная связывается с типом в момент присваивания значения, а не в момент объявления переменной
>>> a = 2000
>>> type(a)
<class 'int'>
>>> a = 'Dynamic Typing'
>>> type(a)
<class 'str'>
>>> a = [1, 2, 3]
>>> type(a)
<class 'list'>
[х] Что такое интерпретируемый язык программирования?
-для исполнения программы не нужен машинный код;
-то есть исходники не нужно компилировать.
-вместо этого программу построчно исполнят интерпретаторы